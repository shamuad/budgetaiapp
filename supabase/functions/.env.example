# Local/CI-only credentials. The key is deliberately fake; the URL points to
# the deterministic Gemini-compatible server started by the integration test.
GEMINI_API_KEY=local-integration-test-key
GEMINI_API_BASE_URL=http://host.docker.internal:54325/v1beta
